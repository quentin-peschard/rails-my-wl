<h1><%= @list.name %></h1>

<%= simple_form_for [@list, @movie, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.input :rating %>
  <%= f.association :movie %>
  <%= f.button :submit %>
<% end %>

<% @bookmarks.each do |bookmark| %>
  <h3><%= bookmark.movie.title %></h3><%=  %>
<% end %>
