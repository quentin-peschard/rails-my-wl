<h1>My lists</h1>

<%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.button :submit %>
<% end %>

<% @lists.each do |list| %>
  <%= link_to list_path(list) do %>
    <%= list.name %>
  <% end %>
  <%= link_to 'X', list_path(list), method: :delete %>
<% end %>
